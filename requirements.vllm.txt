# CosyVoice vLLM Server - 基于 WSL cosyvoice_vllm 环境导出
# Python 3.10 | CUDA 12.6 | PyTorch 2.7.0+cu126
#
# 安装方式:
#   pip install -r requirements.vllm.txt

--extra-index-url https://download.pytorch.org/whl/cu126

# ============================================================
#  PyTorch 核心
# ============================================================
torch==2.7.0
torchaudio==2.7.0
xformers==0.0.30
triton==3.3.0

# ============================================================
#  vLLM 推理引擎
# ============================================================
vllm==0.9.0

# ============================================================
#  ONNX Runtime
# ============================================================
onnxruntime-gpu==1.18.0; sys_platform == 'linux'
onnxruntime==1.18.0; sys_platform == 'darwin' or sys_platform == 'win32'

# ============================================================
#  CosyVoice 模型依赖
#  注意: openai-whisper 由 Dockerfile 单独安装 (--no-build-isolation --no-deps)
# ============================================================
conformer==0.3.2
diffusers==0.29.0
einops==0.8.2
hydra-core==1.3.2
HyperPyYAML==1.2.3
librosa==0.10.2
lightning==2.2.4
omegaconf==2.3.0
pyworld==0.3.4
sentencepiece==0.2.1
soundfile==0.13.1
x-transformers==2.11.24

# ============================================================
#  Transformers / Tokenizer / 模型加载
#  注意: transformers 版本由 Dockerfile 单独控制，此处不锁定，
#  避免与 vllm 依赖冲突（vllm>=0.9.0 要求 transformers>=4.51.1，
#  但 CosyVoice 实际需要 4.48.0，需 --no-deps 强制降级）
# ============================================================
tokenizers==0.21.4
safetensors==0.7.0
modelscope==1.34.0

# ============================================================
#  Web 服务 (FastAPI)
# ============================================================
fastapi==0.128.7
uvicorn==0.40.0
starlette==0.52.1
pydantic==2.12.5

# ============================================================
#  科学计算 / 数据处理
# ============================================================
numpy==1.26.4
scipy==1.15.3
matplotlib==3.7.5
pandas==2.3.3
scikit-learn==1.7.2
pyarrow==18.1.0

# ============================================================
#  通用工具
# ============================================================
gdown==5.1.0
inflect==7.3.1
networkx==3.4.2
protobuf==6.33.5
grpcio==1.78.0
rich==14.3.2
tqdm==4.67.3
wget==3.2
PyYAML==6.0.3
requests==2.32.5
filelock==3.20.3
regex==2026.1.15
